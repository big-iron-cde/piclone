name: Build

on:
  push:
    tags:
      - '*.*.*'
    
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      matrix:
        board: [pico, pico_w, pico_2, pico_2w]
    steps:
    - uses: actions/checkout@v4.1.7
      with:
        fetch-depth: 0

    - name: Clone repo to ~/tools/pico-sdk
      run: git clone -b master --recurse-submodules https://github.com/raspberrypi/pico-sdk.git /home/runner/tools/pico-sdk

    - name: Debug Pico SDK
      run: |
        git -C /home/runner/tools/pico-sdk rev-parse HEAD
        git -C /home/runner/tools/pico-sdk describe --tags --always
        find /home/runner/tools/pico-sdk/src/boards -maxdepth 2 -type f | sort
    - name: Check board files
      run: |
        ls -l /home/runner/tools/pico-sdk/src/boards/include/boards
        find /home/runner/tools/pico-sdk/src/boards -name "pico_2*"

    - name: Install ARM cross-compiler
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc-arm-none-eabi
    
    - name: Create bin output folder
      run: mkdir -p bin

    - name: Build for Each Pico
      run: |
        cd pico-rom-test
        mkdir -p build && cd build
        cmake .. -DPICO_BOARD=${{ matrix.board }}
        make
        cp pico-rom-test.uf2 ../../bin/pico-rom-test-${{ matrix.board }}.uf2
        cd ../
        rm -rf build
        
    - name: Create release
      id: create-release
      uses: softprops/action-gh-release@v3
      with:
        files: |
          bin/pico-rom-test-${{ matrix.board }}.uf2
        make_latest: true

    env:
      PICO_SDK_PATH: /home/runner/tools/pico-sdk


